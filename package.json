{
  "name": "ai-rate-limiter",
  "version": "1.0.0",
  "description": "A general-purpose rate limiter with an AI/LLM token-cost mode, built on Redis. Supports fixed-cost request limiting (sliding window counter) and variable-cost token limiting (token bucket) for LLM API usage.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/unit/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "demo": "node scripts/demo.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
